<div class="HomePage">
  <section class="banner">
    <div class="center">
      <h2><%= raw @page.content_for(:tagline) %></h2>
      <%= render "/refinery/shared/search" %>
    </div>
    <%= image_tag("mapc_logo.svg", class: "logo") %>
  </section>
  <div class="container">
    <section class="content">
      <%= raw @page.content_for(:body) %>
    </section>
  </div>
</div>
