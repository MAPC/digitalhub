<%= render 'refinery/admin/search_header', :url => refinery.announcements_admin_announcements_path %>
<div class='pagination_container'>
  <% if @announcements.any? %>
    <%= render 'announcements' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
