<%= render 'refinery/admin/search_header', :url => refinery.tags_admin_tags_path %>
<div class='pagination_container'>
  <% if @tags.any? %>
    <%= render 'tags' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
