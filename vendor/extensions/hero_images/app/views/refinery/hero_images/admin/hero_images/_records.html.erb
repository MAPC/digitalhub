<%= render 'refinery/admin/search_header', :url => refinery.hero_images_admin_hero_images_path %>
<div class='pagination_container'>
  <% if @hero_images.any? %>
    <%= render 'hero_images' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
